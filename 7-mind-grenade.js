const num1= 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is : ${num1 + num2}`)

}
     addValues();

     //const data = require('./6-alternative-flavor')
require('./7-mind-grenade')// if we have a function in a module we do not have to invoke variable.excuting function
